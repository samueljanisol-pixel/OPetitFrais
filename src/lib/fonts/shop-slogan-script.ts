import { Caveat } from "next/font/google";

/** Slogan boutique (style manuscrit, français). */
export const shopSloganScript = Caveat({
  subsets: ["latin"],
  weight: ["600", "700"],
  display: "swap",
});
